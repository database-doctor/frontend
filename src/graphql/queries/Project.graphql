query GetUserProjects {
  allProjects {
    pid
    name
    createdAt
    createdBy
    dbUrl
  }
}

query GetProjectDetails($pid: Int!) {
  project(pid: $pid) {
    name
    createdAt
    pid
    dbUrl
    createdBy
    users {
      createdAt
      email
      name
      uid
      username
    }
    roles {
      name
      rid
      permissions {
        name
        pid
      }
    }
  }
}
